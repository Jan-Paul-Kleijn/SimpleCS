# SimpleCS

Simple CS
Lightweight XHTML Content System

isHomepage()
isUser
frontendMode()
getIdentity()
is404()
isArticle(slug)
isPage(slug)
isCategory(slug)
isMainCategory(slug)
isEvent(slug)
isSubCategory(slug)
getCatIdBySEF(slug)
getCat(categoryID)
s(name)
l(key)
tabindex(interval)
